$(document).ready(() => {

    // start button clicked, start div disappears, question div pops up, timer starts
    $('#start-btn').on('click', () => {
        $('#start').empty();
        $('#questions').show();
        // timer start
    });































})